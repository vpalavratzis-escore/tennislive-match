import { loadClubRegistry } from "../clubRegistry.js";

function option(value, label) { const el=document.createElement("option"); el.value=value; el.textContent=label; return el; }
function fill(el, items, placeholder) { el.replaceChildren(option("", placeholder), ...items.map(x=>option(x.id,x.name))); el.disabled=!items.length; }
function courtSport(court) { return String(court.sport || "tennis").trim().toLowerCase(); }

export function renderLive() {
  const app=document.getElementById("app"), base=import.meta.env.BASE_URL || "/";
  app.innerHTML=`<div class="wrap"><div class="court-page"><header class="vc-home-nav"><a href="${base}" class="vc-home-brand"><img src="${base}logoText.png" alt="VoxCourt"></a><nav class="vc-home-links"><a href="${base}">Home</a><a class="active" href="${base}live">Find courts</a><a href="/matches/">Match Results</a><a href="/members/manage.html">Members</a></nav></header><main><section class="court-hero"><div class="court-hero-copy"><div class="court-eyebrow">VoxCourt network</div><h1 class="court-title">What do you want <span>to watch?</span></h1><p class="court-intro">Choose a sport first, then find the right area, club and court.</p><div class="sport-choice" id="sportChoice"><button data-sport="tennis">Tennis</button><button data-sport="padel">Padel</button><span>Beach Volleyball <small>Coming soon</small></span><span>Basketball <small>Coming soon</small></span><span>Volleyball <small>Coming soon</small></span></div></div><section class="court-selector-card"><div class="court-selector-top"><div><div class="badge"><i></i> Court selector</div><h2>Find your court</h2><p>Only courts supporting your selected sport are shown.</p></div></div><div class="court-select-grid"><div class="court-field"><label for="selSport">Sport</label><select id="selSport"><option value="">Choose sport</option><option value="tennis">Tennis</option><option value="padel">Padel</option></select></div><div class="court-field"><label for="selCountry">Country / area</label><select id="selCountry" disabled></select></div><div class="court-field"><label for="selCity">City / region</label><select id="selCity" disabled></select></div><div class="court-field"><label for="selClub">Club</label><select id="selClub" disabled></select></div><div class="court-field"><label for="selCourt">Court</label><select id="selCourt" disabled></select></div></div><button id="btnOpen" class="btn primary court-open" disabled>Open court <span>›</span></button><p id="courtMessage" class="court-help">Select Tennis or Padel to begin.</p></section></section></main><footer class="footer premium-footer">© ${new Date().getFullYear()} VoxCourt · Score · Live · Replay</footer></div></div>`;
  const get=id=>app.querySelector(`#${id}`), sport=get("selSport"), country=get("selCountry"), city=get("selCity"), club=get("selClub"), court=get("selCourt"), open=get("btnOpen"), message=get("courtMessage"); let registry={countries:[]};
  const supports=(node,s)=>JSON.stringify(node.cities || node.clubs || node.courts || node).toLowerCase().includes(`\"sport\":\"${s}\"`);
  const currentCountry=()=>registry.countries.find(x=>x.id===country.value), currentCity=()=>currentCountry()?.cities?.find(x=>x.id===city.value), currentClub=()=>currentCity()?.clubs?.find(x=>x.id===club.value), currentCourt=()=>currentClub()?.courts?.find(x=>x.id===court.value);
  function resetFrom(level) { if(level<=1) fill(country,[],"Country / area"); if(level<=2) fill(city,[],"City / region"); if(level<=3) fill(club,[],"Club"); if(level<=4) fill(court,[],"Court"); open.disabled=true; }
  function chooseSport(value) { sport.value=value; resetFrom(1); const items=registry.countries.filter(x=>supports(x,value)); fill(country,items,"Country / area"); message.textContent=items.length?"Choose an area to continue.":`No active ${value} courts are currently registered.`; app.querySelectorAll("[data-sport]").forEach(x=>x.classList.toggle("active",x.dataset.sport===value)); }
  app.querySelectorAll("[data-sport]").forEach(x=>x.addEventListener("click",()=>chooseSport(x.dataset.sport)));
  sport.addEventListener("change",()=>chooseSport(sport.value));
  country.addEventListener("change",()=>{ resetFrom(2); fill(city,(currentCountry()?.cities||[]).filter(x=>supports(x,sport.value)),"City / region"); });
  city.addEventListener("change",()=>{ resetFrom(3); fill(club,(currentCity()?.clubs||[]).filter(x=>(x.courts||[]).some(c=>courtSport(c)===sport.value)),"Club"); });
  club.addEventListener("change",()=>{ resetFrom(4); fill(court,(currentClub()?.courts||[]).filter(c=>courtSport(c)===sport.value),"Court"); });
  court.addEventListener("change",()=>{ open.disabled=!currentCourt(); });
  open.addEventListener("click",()=>{ if(currentCourt()) location.href=`${base}?p=${encodeURIComponent(`/${country.value}/${city.value}/${club.value}/${court.value}`)}`; });
  loadClubRegistry().then(data=>{registry=data; message.textContent="Select Tennis or Padel to begin.";}).catch(()=>{message.textContent="Court registry is temporarily unavailable. Please try again.";});
}
